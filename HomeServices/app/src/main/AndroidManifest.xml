<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.angemichaella.homeservices">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/houseicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/repair_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Welcome" />
        <activity android:name=".WelcomeHomeOwner" />
        <activity android:name=".WelcomeAdmin" />
        <activity android:name=".WelcomeServiceProvider" />
        <activity android:name=".User_list_layout" />
        <activity android:name=".UserList"></activity>
    </application>

</manifest>